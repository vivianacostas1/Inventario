info:
  name: "Listar Accionistas-productos "
  type: http

http:
  method: GET
  url: http://localhost:3000/api/shareholder-products
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
