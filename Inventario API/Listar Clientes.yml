info:
  name: Listar Clientes
  type: http

http:
  method: GET
  url: http://localhost:3000/api/customers
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​