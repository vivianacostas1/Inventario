info:
  name: Listar Categorias
  type: http

http:
  method: GET
  url: http://localhost:3000/api/categories
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
