info:
  name: Listar Usuarios
  type: http

http:
  method: GET
  url: http://localhost:3000/api/users
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
