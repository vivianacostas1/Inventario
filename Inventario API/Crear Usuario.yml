info:
  name: Crear Usuario
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/users
  body:
    type: json
    data: |-
      {
        "name": "Prueba Bruno1",
        "email": "prueba2@test.com",
        "passwordHash": "123456",
        "role": "ADMIN"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
