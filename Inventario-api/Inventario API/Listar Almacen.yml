info:
  name: Listar Almacen
  type: http

http:
  method: GET
  url: http://localhost:3000/api/warehouses
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
