info:
  name: Crear Categoria
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/categories
  body:
    type: json
    data: |-
      {
        "name": "Electordomestivo",
        "description": "electroodomesticos"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​